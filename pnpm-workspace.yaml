# pnpm workspace configuration
packages:
  - '.'
  - 'packages/*'
allowBuilds:
  esbuild: true
  unrs-resolver: true

# Force patched transitive deps (Snyk SNYK-JS-FASTURI-17675102)
overrides:
  fast-uri: ^3.1.3
